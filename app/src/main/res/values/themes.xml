<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme.OverlapSystemBar" parent="Theme.MaterialComponents.DayNight.NoActionBar" />
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="windowActionModeOverlay">true</item>
      <!--  <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>-->

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorPrimary</item>
        <item name="colorSecondaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- used to change the color of menu(overflow) item text (suppose if we use app:showAsAction="always|withText"
        and set no icon, then only text will appear) -->
        <item name="android:actionMenuTextColor">@color/white</item>

       <!-- <item name="actionBarStyle">@style/Toolbar</item>-->

        <!--used to change action bar settings icon color(three dots)  -->
        <item name="android:textColorSecondary">@color/white</item>

        <item name="actionModeBackground">@color/colorPrimary</item>
        <item name="actionModeStyle">@style/ContextualActionModeTheme</item>
        <item name="actionModeCloseDrawable">@drawable/ic_baseline_close_24</item>
        <item name="actionModeSelectAllDrawable">@drawable/ic_baseline_close_24</item>


        <!-- Customize your theme here. -->
    </style>

  <!--  <style name="Toolbar" parent="Widget.MaterialComponents.Badge" />
-->
    <!-- ActionBar styles -->

<!--
    <style name="Toolbar" parent="Widget.MaterialComponents.Badge">
        <item name="titleTextColor">@color/white</item>
        <item name="backgroundColor">@color/colorPrimary</item>
        <item name="android:textColor">@color/white</item>
        <item name="iconTint">@android:color/white</item>
    </style>
-->

    <style name="ContextualActionModeTheme" parent="@style/Widget.AppCompat.ActionMode">
        <item name="titleTextStyle">@style/actionModeTitleColor</item>
        <item name="actionModeCloseButtonStyle">@style/actionModeCloseButtonStyle</item>
    </style>

    <style name="actionModeTitleColor" parent="TextAppearance.AppCompat.Widget.ActionMode.Title">
        <item name="android:textColor">@color/white</item>
        <item name="android:color">@color/white</item>
    </style>

    <style name="actionModeCloseButtonStyle" parent="Base.Widget.AppCompat.ActionButton.CloseMode">
        <item name="android:background">@color/white</item>
        <item name="android:minWidth">56dp</item>
    </style>


</resources>